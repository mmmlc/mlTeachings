# install R https://cran.r-project.org/bin/windows/base/
# install R Studio (R IDE) https://www.rstudio.com/products/rstudio/download/

# alternatively, you can work here https://rstudio.cloud/

# copy-paste the following R instructions

1.
# data manipulation
install.packages("tidyverse")

2.
# general ML
install.packages("caret")

3.
# others in support
install.packages(c('rmarkdown',
                   'e1071',
                   'mlbench',
                   'ggthemes',
                   'assertthat',
                   'kernlab',
                   'rpart',
                   'rattle',
                   'kknn',
                   'randomForest',
                   'nnet'
                   'RSNNS',
                   'metrics')
)



# install the latest available release of Python for 64 bit OS https://www.howtogeek.com/197947/how-to-install-python-on-windows/
# if you do not have enough bits don't even try to run what's below. it won't work. othervise try, succed and then tell me how to do it plz.

# copy-paste the following R instructions

1.
install.packages("reticulate")

2.
# neural nets
install.packages("tensorflow")
install.packages("keras")

3.
# install python packages
tensorflow::install_tensorflow()
keras::install_keras()

# bonus

# if you want to run jupyer notebooks onl your machine (REQUIRES PYTHON INSTALLED)

1. copy-paste the following R instructions

install.packages(c('repr',
                   'IRdisplay',
                   'evaluate',
                   'crayon',
                   'pbdZMQ',
                   'devtools',
                   'uuid',
                   'digest'))
devtools::install_github('IRkernel/IRkernel')
IRkernel::installspec(user = FALSE)

2. IN THE CMD LINE, (opened as administrator) write the following instructions (those within quotes, but without quotes)

a. 
# install jupyter
	a.1 " pip install jupyter " [if you wish to use jupyter notebooks]
	a.2 " pip install jupyterlab " [if you wish to use jupyterlab]

b.
# be sure to be in the right hard drive
	"C:" [or whatever is your hard drive called]

c.
# start jupyter.
	c.1 " jupyter-notebook "
	c.2 " jupyter-lab "
	
d.
# to close the connection when you are done
	hit " CTRL+C " twice

3. open your browser (magic happens here!)	
4. source for the .ipynb file in its folder
5. select R kernel
6. enjoy locally

	